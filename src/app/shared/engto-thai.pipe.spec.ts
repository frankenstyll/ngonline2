import { EngtoThaiPipe } from './engto-thai.pipe';

describe('EngtoThaiPipe', () => {
  it('create an instance', () => {
    const pipe = new EngtoThaiPipe();
    expect(pipe).toBeTruthy();
  });
});
